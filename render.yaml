services:
  - type: web
    name: quiz
    runtime: node
    plan: free
    buildCommand: "true"
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: HOST_PASSWORD
        sync: false
